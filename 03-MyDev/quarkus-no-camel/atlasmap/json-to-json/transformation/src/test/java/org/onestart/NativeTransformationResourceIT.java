package org.onestart;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeTransformationResourceIT extends TransformationResourceTest {

    // Execute the same tests but in native mode.
}